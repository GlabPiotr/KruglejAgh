export class Student {
  name: string;
  lastName: string;
  identityNumber: number;

  constructor(name: string, lastName: string, identityNumber: number) {
    this.name = name;
    this.lastName = lastName;
    this.identityNumber = identityNumber;
  }
}
